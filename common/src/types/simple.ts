export type FontData = { name: string; url: string; pixelSize: number; glyphRange: number };
export type UserProfile = { username: string };
export type InitState = { userProfile: UserProfile; roomId: number }
